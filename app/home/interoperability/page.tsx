import { lusitana } from "@/app/ui/font";

export default function Interoperability() {
    return (
        <p className={`text-[44px] ${lusitana.className}`}>Interoperability Test Cases</p>
    );
}